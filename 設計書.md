# 勤務管理システム - 設計書

## 目次

1. [システム概要](#システム概要)
2. [技術スタック](#技術スタック)
3. [アーキテクチャ](#アーキテクチャ)
4. [データベース設計](#データベース設計)
5. [API設計](#api設計)
6. [機能仕様](#機能仕様)
7. [システム構成](#システム構成)

---

## システム概要

### プロジェクト名

勤務管理システム（Timesheet Management System）

### 概要

社員の勤務時間を管理するWebアプリケーション。出勤・退勤時刻の記録、勤務表の生成、PDF/Excel形式でのエクスポート機能を提供する。

### 主な機能

- **勤務管理**: 出勤・退勤時刻の記録とリアルタイム計算
- **勤務表表示**: 月単位での勤務実績表示
- **レポート生成**: PDF、Excel形式での勤務表エクスポート
- **セキュリティ機能**: ユーザー認証、監査ログ、IP制限、UA（User-Agent）ブラックリスト
- **休日管理**: 国家休日、会社休日の管理

---

## 技術スタック

### バックエンド

- **言語**: Kotlin 2.3.0-RC
- **フレームワーク**: Spring Boot 3.5.7
- **データベース**: PostgreSQL 42.7.8
- **ORM/QueryBuilder**: MyBatis
- **DB マイグレーション**: Flyway 11.18.0
- **PDF生成**: Apache PDFBox
- **Excel操作**: Apache POI
- **ビルド**: Maven 3.x
- **Java**: OpenJDK 21

### フロントエンド

- **マークアップ**: HTML5, Thymeleaf
- **スタイル**: CSS3
- **スクリプト**: JavaScript (Vanilla)
- **デバイス対応**: レスポンシブデザイン

### インフラ

- **デプロイ**: Docker, Docker Compose
- **WAS**: Apache Tomcat (embedded)
- **ロードバランシング**: 未設定
- **キャッシング**: Spring Cache (in-memory)
- **非同期処理**: Spring Async

---

## アーキテクチャ

### レイヤー構成

```
┌─────────────────────────────────────────┐
│         プレゼンテーション層              │ (HTML, JavaScript, CSS)
├─────────────────────────────────────────┤
│         コントローラー層                  │ (Controller, RestController)
├─────────────────────────────────────────┤
│         ビジネスロジック層                │ (Service, ServiceImpl)
├─────────────────────────────────────────┤
│         データアクセス層                  │ (Mapper - MyBatis)
├─────────────────────────────────────────┤
│         データベース層                    │ (PostgreSQL)
└─────────────────────────────────────────┘
```

### 主要パッケージ構成

```
com.example.demo/
├── controller/              # HTTPリクエスト処理
│   ├── TimesheetController.kt
│   ├── TimesheetReportController.kt
│   ├── CalendarHolidayController.kt
│   └── GeoIpDebugController.kt
├── service/                 # ビジネスロジック
│   ├── TimesheetService.kt
│   ├── TimesheetSummaryService.kt
│   ├── ReportService.kt
│   ├── TimesheetValidator.kt
│   ├── CalendarHolidayService.kt
│   ├── GeoIpCountryService.kt
│   ├── UaBlacklistService.kt
│   ├── BlacklistEventService.kt
│   └── ReportJobService.kt
├── mapper/                  # MyBatis インターフェース
│   ├── TimesheetEntryMapper.kt
│   ├── ReportJobMapper.kt
│   ├── CalendarHolidayMapper.kt
│   ├── BlacklistIpMapper.kt
│   ├── WhitelistIpMapper.kt
│   ├── BlacklistEventMapper.kt
│   ├── UaBlacklistRuleMapper.kt
│   └── AccessLogMapper.kt
├── model/                   # データモデル
│   ├── TimesheetEntry.kt
│   ├── ReportJob.kt
│   ├── CalendarHoliday.kt
│   ├── BlacklistIp.kt
│   ├── WhitelistIp.kt
│   ├── BlacklistEvent.kt
│   ├── UaBlacklistRule.kt
│   ├── AccessLog.kt
│   └── IpLatestPath.kt
├── config/                  # 設定・フィルター
│   ├── SecurityConfig.kt
│   ├── MyBatisConfig.kt
│   ├── AsyncConfig.kt
│   ├── SecurityAuditFilter.kt
│   ├── RateLimitFilter.kt
│   ├── LoginRateLimitFilter.kt
│   ├── NoCacheFilter.kt
│   ├── DuplicateSessionExceptionFilter.kt
│   ├── GlobalExceptionHandler.kt
│   └── CustomAuthenticationFailureHandler.kt
├── constants/               # 定数定義
│   └── ApplicationConstants.kt
├── util/                    # ユーティリティ
│   ├── TimesheetGenerator.kt
│   ├── TimesheetEvaluator.kt
│   └── その他の補助関数
├── job/                     # バッチ処理
│   └── 非同期ジョブ実装
└── DemoApplication.kt       # アプリケーションエントリーポイント
```

---

## データベース設計

### ER図の概要

```
┌──────────────────────────┐
│   timesheet_entries      │ (勤務実績)
├──────────────────────────┤
│ id (PK)                  │
│ work_date                │
│ user_name                │
│ start_time               │
│ end_time                 │
│ note                     │
│ break_minutes            │
│ duration_minutes         │
│ working_minutes          │
│ holiday_work             │
│ work_location            │
│ irregular_work_type      │
│ irregular_work_desc      │
│ irregular_work_data      │
│ late_time                │
│ late_desc                │
│ early_time               │
│ early_desc               │
│ free_note (新)           │
│ paid_leave               │
│ version (楽観ロック)      │
│ created_at               │
│ updated_at               │
└──────────────────────────┘
         │
         │ 1:N
         └─→ ┌──────────────────────────┐
             │   report_jobs            │ (レポート生成ジョブ)
             ├──────────────────────────┤
             │ id (PK)                  │
             │ username                 │
             │ from_date                │
             │ to_date                  │
             │ format (PDF/Excel)       │
             │ status (PENDING/...)     │
             │ file_path                │
             │ error_message            │
             │ created_at               │
             │ updated_at               │
             └──────────────────────────┘

┌──────────────────────────┐
│   calendar_holidays      │ (休日管理)
├──────────────────────────┤
│ id (PK)                  │
│ holiday_date             │
│ holiday_name             │
│ is_company_holiday       │
│ created_at               │
│ updated_at               │
└──────────────────────────┘

┌──────────────────────────┐
│   blacklist_ips          │ (IP ブラックリスト)
├──────────────────────────┤
│ id (PK)                  │
│ ip_address               │
│ reason                   │
│ deleted                  │
│ created_at               │
│ updated_at               │
└──────────────────────────┘

┌──────────────────────────┐
│   whitelist_ips          │ (IP ホワイトリスト)
├──────────────────────────┤
│ id (PK)                  │
│ ip_address               │
│ description              │
│ created_at               │
│ updated_at               │
└──────────────────────────┘

┌──────────────────────────┐
│   ua_blacklist_rules     │ (User-Agent ルール)
├──────────────────────────┤
│ id (PK)                  │
│ user_agent_pattern       │
│ rule_type                │
│ created_at               │
│ updated_at               │
└──────────────────────────┘

┌──────────────────────────┐
│   blacklist_events       │ (セキュリティイベント)
├──────────────────────────┤
│ id (PK)                  │
│ event_type               │
│ ip_address               │
│ user_agent               │
│ username                 │
│ reason                   │
│ created_at               │
└──────────────────────────┘

┌──────────────────────────┐
│   access_logs            │ (アクセスログ)
├──────────────────────────┤
│ id (PK)                  │
│ created_at               │
│ request_id (UUID)        │
│ method                   │
│ path                     │
│ query                    │
│ status                   │
│ duration_ms              │
│ remote_ip                │
│ user_agent               │
│ referer                  │
│ username                 │
│ request_bytes            │
│ response_bytes           │
└──────────────────────────┘
```

### 主要テーブル詳細

#### timesheet_entries テーブル

| カラム名                | 型         | 説明           | 備考                  |
|---------------------|-----------|--------------|---------------------|
| id                  | BIGINT    | 主キー          | Auto Increment      |
| work_date           | DATE      | 勤務日          | 一意インデックス(user_name) |
| user_name           | VARCHAR   | ユーザー名        | インデックス              |
| start_time          | TIME      | 出勤時刻         | Nullable            |
| end_time            | TIME      | 退勤時刻         | Nullable            |
| note                | TEXT      | 備考           | Nullable            |
| free_note           | TEXT      | 自由備考（新）      | Nullable            |
| break_minutes       | INT       | 休憩時間（分）      | Nullable            |
| duration_minutes    | INT       | 稼働時間（分）      | 計算値                 |
| working_minutes     | INT       | 実働時間（分）      | 計算値                 |
| holiday_work        | BOOLEAN   | 休日出勤フラグ      | Default: false      |
| work_location       | VARCHAR   | 勤務地区分        | 出社/在宅               |
| irregular_work_type | VARCHAR   | 変則勤務種別       | 有給/特別休暇等            |
| irregular_work_desc | VARCHAR   | 変則勤務説明       | Nullable            |
| irregular_work_data | TEXT      | 複数変則勤務（JSON） | Nullable            |
| late_time           | VARCHAR   | 遅刻時間         | Nullable            |
| late_desc           | VARCHAR   | 遅刻説明         | Nullable            |
| early_time          | VARCHAR   | 早退時間         | Nullable            |
| early_desc          | VARCHAR   | 早退説明         | Nullable            |
| paid_leave          | VARCHAR   | 有給消化フラグ      | "有給" or null        |
| version             | INT       | 楽観ロック用バージョン  | 同時更新検出              |
| created_at          | TIMESTAMP | 作成日時         | タイムゾーン付き            |
| updated_at          | TIMESTAMP | 更新日時         | タイムゾーン付き            |

#### report_jobs テーブル

| カラム名          | 型         | 説明       | 備考                                  |
|---------------|-----------|----------|-------------------------------------|
| id            | BIGINT    | 主キー      | Auto Increment                      |
| username      | VARCHAR   | ユーザー名    | インデックス                              |
| from_date     | DATE      | 開始日      |                                     |
| to_date       | DATE      | 終了日      |                                     |
| format        | VARCHAR   | ファイル形式   | PDF / EXCEL                         |
| status        | VARCHAR   | 処理ステータス  | PENDING/PROCESSING/COMPLETED/FAILED |
| file_path     | VARCHAR   | ファイルパス   | S3 URL等                             |
| error_message | TEXT      | エラーメッセージ | Nullable                            |
| created_at    | TIMESTAMP | 作成日時     |                                     |
| updated_at    | TIMESTAMP | 更新日時     |                                     |

---

## API設計

### 1. 勤務時間API

#### 出勤記録

```
POST /timesheet/api/clock-in
認証: 必須
リクエスト: なし
レスポンス: TimesheetEntry
目的: 出勤時刻を記録
```

#### 退勤記録

```
POST /timesheet/api/clock-out
認証: 必須
リクエスト: なし
レスポンス: TimesheetEntry
目的: 退勤時刻を記録
```

#### 当日の勤務実績取得

```
GET /timesheet/api/today
認証: 必須
リクエスト: なし
レスポンス: TimesheetEntry? (null の場合あり)
目的: 本日の勤務実績を取得
```

#### 期間の勤務実績取得

```
GET /timesheet/api
認証: 必須
パラメータ:
  - from: LocalDate (YYYY-MM-DD)
  - to: LocalDate (YYYY-MM-DD, optional)
レスポンス: List<TimesheetEntry>
目的: 指定期間の勤務実績一覧を取得
```

#### 備考の更新

```
POST /timesheet/api/note
認証: 必須
リクエスト: { "note": "string" }
レスポンス: TimesheetEntry
目的: 当日の備考を更新
```

#### 備考の保存（Beacon API）

```
POST /timesheet/api/note/saveBeacon
認証: 必須
パラメータ:
  - note: String? (optional)
レスポンス: TimesheetEntry
目的: ページ離脱時の非同期保存
```

#### 休憩時間の更新

```
POST /timesheet/api/break
認証: 必須
リクエスト: { "minutes": integer }
レスポンス: TimesheetEntry
目的: 休憩時間を更新
```

#### 自由備考の更新

```
POST /timesheet/api/free-note
認証: 必須
リクエスト: { "freeNote": "string" }
レスポンス: TimesheetEntry
目的: 自由備考を更新（新機能）
```

#### リアルタイム更新のSSE接続

```
GET /timesheet/api/events/subscribe
認証: 必須
Content-Type: text/event-stream
目的: 勤務実績の更新をリアルタイム配信
```

### 2. レポート生成API

#### PDF生成（非同期）

```
POST /timesheet/api/report/pdf
認証: 必須
パラメータ:
  - from: LocalDate (YYYY-MM-DD)
  - to: LocalDate (YYYY-MM-DD)
  - download: boolean (optional, default: true)
レスポンス: { "jobId": string, "status": string }
目的: 勤務表のPDF生成を非同期で開始
```

#### Excel生成（非同期）

```
POST /timesheet/api/report/excel
認証: 必須
パラメータ:
  - from: LocalDate (YYYY-MM-DD)
  - to: LocalDate (YYYY-MM-DD)
  - download: boolean (optional, default: true)
レスポンス: { "jobId": string, "status": string }
目的: 勤務表のExcel生成を非同期で開始
```

#### ジョブステータス確認

```
GET /timesheet/api/report/job/{jobId}
認証: 必須
レスポンス: ReportJob
目的: 生成ジョブのステータスを取得
```

### 3. 休日管理API

#### 休日一覧取得

```
GET /calendar/holidays
認証: 必須
パラメータ:
  - year: int
レスポンス: List<CalendarHoliday>
目的: 指定年の休日一覧を取得
```

#### 休日追加

```
POST /calendar/holidays
認証: 必須（管理者）
リクエスト: { "holidayDate": "YYYY-MM-DD", "holidayName": "string", "isCompanyHoliday": boolean }
レスポンス: CalendarHoliday
目的: 新しい休日を追加
```

### 4. セキュリティAPI（管理画面）

#### IP ブラックリスト管理

```
GET /api/manage/blacklist-ips
POST /api/manage/blacklist-ips
DELETE /api/manage/blacklist-ips/{id}
```

#### IP ホワイトリスト管理

```
GET /api/manage/whitelist-ips
POST /api/manage/whitelist-ips
DELETE /api/manage/whitelist-ips/{id}
```

#### User-Agent ルール管理

```
GET /api/manage/ua-blacklist
POST /api/manage/ua-blacklist
DELETE /api/manage/ua-blacklist/{id}
```

---

## 機能仕様

### 1. 勤務管理機能

#### 1.1 出勤・退勤管理

- **出勤**: ユーザーが出勤ボタンをクリックすると `startTime` を記録
- **退勤**: ユーザーが退勤ボタンをクリックすると `endTime` を記録
- **ステータス判定**:
    - `startTime == null`: 未出勤
    - `startTime != null && endTime == null`: 勤務中
    - `startTime != null && endTime != null`: 勤務完了

#### 1.2 時間計算

- **稼働時間** (`durationMinutes`): `endTime - startTime`
- **実働時間** (`workingMinutes`): `durationMinutes - breakMinutes`
- **計算処理**: `TimesheetEvaluator.evaluate()` で実施

#### 1.3 同時実行制御

- **楽観ロック**: `version` カラムで同時更新を検出
- **更新失敗時**: `TimesheetConflictException` を投げて再試行

#### 1.4 備考機能

- **通常備考** (`note`): 勤務表の備考列に表示
- **自由備考** (`freeNote`): 別モーダルで編集（新機能）
- **ユーニス勤務表対応**:
    - 通常備考: AB列に「・備考: 」形式で出力
    - 自由備考: AB列に直接出力（「・備考: 」不要）
- **Excel/PDF**: 自由備考は不要（通常備考のみ）

#### 1.5 変則勤務

- **種別**: 有給休暇、特別休暇、欠勤、振替出勤、振替休日、休日出勤
- **複数対応**: `irregularWorkData` (JSON形式) で複数の変則勤務を管理

#### 1.6 遅刻・早退

- **遅刻**: `lateTime`, `lateDesc`
- **早退**: `earlyTime`, `earlyDesc`

---

### 2. 勤務表表示機能

#### 2.1 月単位の勤務表

- **URL**: `/timesheet?month=YYYY-MM`
- **表示内容**:
  | 日付 | 曜日 | 備考 | 出勤時間 | 退勤時間 | 休憩 | 稼働 | 実働 |
  |------|------|------|---------|---------|------|------|------|
  | 1 | 月 | 〇 | 09:00 | 18:00 | 60 | 480 | 420 |

#### 2.2 レスポンシブ対応

- **PC版**: フルレイアウト（全列表示）
- **スマホ版**: カラムを最適化（列幅調整）
    - 問題事項:
        - 列位置のズレ (resolved)
        - 全画面表示後の実働合計の消失 (resolved)

#### 2.3 全画面表示機能

- **ボタン**: 勤務表の上部に「全画面表示」ボタン
- **挙動**: モーダルで勤務表を全画面表示
- **終了**: Escキーでモーダルを閉じる
- **問題**: 閉じた後に実働合計が左横に表示される (fixed)

---

### 3. レポート生成機能

#### 3.1 Excel生成

- **形式**: .xlsx (Microsoft Excel 2007+)
- **内容**:
    - ユーザー名、期間
    - 勤務実績（全日付）
    - 計算済み統計
- **スタイル**:
    - タイトル: 14pt Bold, 中央揃え
    - ヘッダー: グレー背景, Bold
    - セルボーダー: すべてのセルに枠線
    - オートサイズ: 列幅自動調整
- **休日位置**:
    - `START`: 最初
    - `MIDDLE`: 中央（デフォルト）
    - `END`: 最後

#### 3.2 PDF生成

- **形式**: PDF (ISO 32000)
- **内容**:
    - ユーザー名
    - 期間
    - 勤務実績（表形式）
    - 会社名「ユーニスイースト株式会社」
- **フォント**: 日本語対応フォント (embedded)
- **ページ設定**: A4横向き

#### 3.3 生成処理

- **非同期処理**: `@Async` で別スレッドで実行
- **ステータス遷移**: `PENDING` → `PROCESSING` → `COMPLETED` (or `FAILED`)
- **エラーハンドリング**: `errorMessage` にエラー内容を記録
- **ファイル保存**: ローカルファイルシステムまたはS3

---

### 4. セキュリティ機能

#### 4.1 認証・認可

- **認証機構**: Spring Security (HTTP Basic / Form Login)
- **セッション管理**: Spring Session
- **重複ログイン防止**: `DuplicateSessionExceptionFilter`

#### 4.2 IP制限

- **ブラックリスト**: 危険なIPアドレスをブロック
- **ホワイトリスト**: 許可されたIPアドレスのみ許可（オプション）
- **監査**: `BlacklistEvent` に記録

#### 4.3 User-Agent（UA）フィルター

- **ルール**: User-Agentパターンでアクセスをフィルター
- **用途**: 特定ボットやマルウェアをブロック
- **監査**: イベント記録

#### 4.4 レート制限

- **ログイン**: `LoginRateLimitFilter` で制限
- **全体**: `RateLimitFilter` で制限
- **閾値**: 設定可能

#### 4.5 監査ログ

- **アクセスログ**: すべてのHTTPリクエストを記録
    - method, path, query, status, duration
    - remote_ip, user_agent, username
    - request_bytes, response_bytes
- **セキュリティイベント**: IP/UA ブロック時に記録

#### 4.6 キャッシュ制御

- **フィルター**: `NoCacheFilter`
- **設定**: `Cache-Control: no-store, no-cache, must-revalidate`
- **目的**: 機密情報のキャッシュ防止

---

### 5. その他機能

#### 5.1 休日管理

- **国家休日**: 予め定義された国家休日
- **会社休日**: カスタム追加可能
- **外部API**: 独立行政法人 国立青少年教育振興機構等との連携（オプション）

#### 5.2 地理情報

- `GeoIpCountryService`: IPアドレスから国を特定（デバッグ用）

#### 5.3 非同期処理

- `@Async` を使用した非同期タスク実行
- `ReportService`: レポート生成
- `TimesheetService`: イベント発行

#### 5.4 キャッシング

- `@Cacheable`: メソッドレベルのキャッシング
- `ConcurrentHashMap`: 手動キャッシュ実装

---

## システム構成

### ファイル構成

#### コントローラー層

**MainController.kt**

- ホーム画面、勤務表画面のレンダリング
- 月パラメータの処理

**TimesheetController.kt**

- 出勤・退勤API
- 勤務実績の取得・更新API
- SSE通知

**TimesheetReportController.kt**

- PDF/Excel生成API
- ジョブステータス確認API

**CalendarHolidayController.kt**

- 休日管理API

**GeoIpDebugController.kt**

- IP→国マッピング（デバッグ用）

#### ビジネスロジック層

**TimesheetService.kt** (348行)

- 出勤・退勤処理
- 勤務実績の取得・更新
- イベント発行

**TimesheetSummaryService.kt**

- 勤務サマリー計算（月合計等）

**ReportService.kt** (796行)

- Excel生成
- PDF生成
- 休日データの取得

**TimesheetValidator.kt**

- 入力値の検証

**CalendarHolidayService.kt**

- 休日の取得・管理

**GeoIpCountryService.kt**

- IPジオロケーション情報

**UaBlacklistService.kt**

- User-Agentルール管理

**BlacklistEventService.kt**

- セキュリティイベント記録

**ReportJobService.kt**

- レポートジョブ管理

#### ユーティリティ

**TimesheetGenerator.kt**

- 勤務表の日付生成
- 年月フォーマット

**TimesheetEvaluator.kt**

- 時間計算と検証

#### フィルター・設定

**SecurityConfig.kt**

- Spring Security設定
- 認証・認可ルール

**MyBatisConfig.kt**

- MyBatis設定

**AsyncConfig.kt**

- @Async実行スレッドプール設定

**SecurityAuditFilter.kt**

- IP/UA監査

**RateLimitFilter.kt**

- レート制限

**NoCacheFilter.kt**

- キャッシュ制御

**GlobalExceptionHandler.kt**

- 例外のハンドリング

---

## 開発ガイドライン

### 命名規則

- **Kotlin**: camelCase（変数・メソッド）, PascalCase（クラス）
- **SQL**: snake_case（テーブル・カラム）
- **API**: kebab-case（パスパラメータ）

### エラーハンドリング

- 例外: `TimesheetConflictException`, `TimesheetNotFoundException`
- HTTP Status: 適切なステータスコードを返す（400, 404, 500等）

### トランザクション管理

- `@Transactional`: データベース操作の一貫性を保証
- 楽観ロックで同時実行制御

### ログ出力

- `LoggerFactory.getLogger()` で取得
- レベル: DEBUG, INFO, WARN, ERROR

### テスト

- ユニットテスト: JUnit 5
- 統合テスト: TestContainers (PostgreSQL)

---

## デプロイメント

### ビルド

```bash
mvn clean package
```

### 実行

```bash
java -jar target/app-0.0.1-SNAPSHOT.jar
```

### Docker

```bash
docker-compose up -d
```

### 環境変数

- `SPRING_DATASOURCE_URL`: PostgreSQL接続URL
- `SPRING_DATASOURCE_USERNAME`: DBユーザー名
- `SPRING_DATASOURCE_PASSWORD`: DBパスワード
- `SPRING_PROFILES_ACTIVE`: prod / dev

---

## 既知の問題と解決状況

### 解決済み

- [x] スマホ版の列のズレ
- [x] 全画面表示後の実働合計消失
- [x] モーダルの位置ズレ
- [x] 自由備考の保存
- [x] キャッシュ設定（noStore）

### 進行中

- アクセシビリティの向上
- パフォーマンス最適化

---

## 参考リンク

- [Spring Boot 公式ドキュメント](https://spring.io/projects/spring-boot)
- [Kotlin 言語リファレンス](https://kotlinlang.org/docs/reference/)
- [MyBatis ドキュメント](https://mybatis.org/mybatis-3/)
- [PostgreSQL ドキュメント](https://www.postgresql.org/docs/)

---

## 最終更新日

2026年1月9日

## ドキュメント作成者

GitHub Copilot

