<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="60">
    <Properties>
        <Property name="PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg | rid=%X{requestId}
            uid=%X{userId} ip=%X{clientIp} %X{method} %X{uri}%n%throwable
        </Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${PATTERN}"/>
        </Console>

        <JDBC name="AuditDB" tableName="audit_log" ignoreExceptions="true">
            <DriverManager connectionString="${env:LOG_DB_URL:-${spring:spring.datasource.url:-jdbc:postgresql://db:5432/demo_db}}"
                           userName="${env:LOG_DB_USER:-${spring:spring.datasource.username:-demo_user}}"
                           password="${env:LOG_DB_PASS:-${spring:spring.datasource.password:-demo_pass}}"
                           driverClassName="org.postgresql.Driver"/>
            <Column name="event_time" isEventTimestamp="true"/>
            <Column name="level" pattern="%level"/>
            <Column name="logger" pattern="%logger"/>
            <Column name="message" pattern="%message"/>
            <Column name="thread" pattern="%thread"/>
            <Column name="exception" pattern="%ex{full}"/>
            <Column name="request_id" pattern="%X{requestId}"/>
            <Column name="user_id" pattern="%X{userId}"/>
            <Column name="client_ip" pattern="%X{clientIp}"/>
            <Column name="method" pattern="%X{method}"/>
            <Column name="uri" pattern="%X{uri}"/>
        </JDBC>

        <Async name="AsyncConsole" bufferSize="1024">
            <AppenderRef ref="Console"/>
        </Async>

        <Async name="AsyncAuditTargets" bufferSize="1024">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AuditDB"/>
        </Async>
    </Appenders>

    <Loggers>
        <!-- Application loggers -->
        <Logger name="com.example.demo" level="info" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <!-- Dedicated audit logger -->
        <Logger name="AUDIT" level="info" additivity="false">
            <AppenderRef ref="AsyncAuditTargets"/>
        </Logger>

        <Root level="warn">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
