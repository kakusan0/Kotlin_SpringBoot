<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.BlacklistEventMapper">

    <insert id="insert" parameterType="com.example.demo.model.BlacklistEvent" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO
        blacklist_events ( created_at, request_id, ip_address, method, path, status, user_agent, referer, reason
        , source)
        VALUES
        (
        <choose>
            <when test="createdAt != null">#{createdAt,jdbcType=TIMESTAMP,javaType=java.time.OffsetDateTime}</when>
            <otherwise>CURRENT_TIMESTAMP</otherwise>
        </choose>
        , #{requestId,jdbcType=VARCHAR}
        , #{ipAddress,jdbcType=VARCHAR}, #{method,jdbcType=VARCHAR}, #{path,jdbcType=VARCHAR}
        , #{status,jdbcType=INTEGER}, #{userAgent,jdbcType=VARCHAR}, #{referer,jdbcType=VARCHAR}
        , #{reason,jdbcType=VARCHAR}, #{source,jdbcType=VARCHAR})
    </insert>

    <insert id="insertBulk" parameterType="list">
        INSERT INTO
        blacklist_events (created_at, request_id, ip_address, method, path, status, user_agent, referer, reason, source)
        VALUES
        <foreach collection="list" item="event" separator=",">
            (
            <choose>
                <when test="event.createdAt != null">#{event.createdAt,jdbcType=TIMESTAMP,javaType=java.time.OffsetDateTime}</when>
                <otherwise>CURRENT_TIMESTAMP</otherwise>
            </choose>
            , #{event.requestId,jdbcType=VARCHAR}
            , #{event.ipAddress,jdbcType=VARCHAR}, #{event.method,jdbcType=VARCHAR}, #{event.path,jdbcType=VARCHAR}
            , #{event.status,jdbcType=INTEGER}, #{event.userAgent,jdbcType=VARCHAR}, #{event.referer,jdbcType=VARCHAR}
            , #{event.reason,jdbcType=VARCHAR}, #{event.source,jdbcType=VARCHAR}
            )
        </foreach>
    </insert>

</mapper>
