<?xml version="1.0" encoding="UTF-8"?>
<!--
  依存関係バージョンチェックのルール設定
  - RC, Beta, Alpha, SNAPSHOT などの不安定版を除外
  - 特定のライブラリを除外
-->
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         comparisonMethod="maven"
         xmlns="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0"
         xsi:schemaLocation="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0
                             https://www.mojohaus.org/versions-maven-plugin/xsd/rule-2.0.0.xsd">

    <!-- 無視するバージョンパターン（RC, Beta, Alpha, M, SNAPSHOT を除外） -->
    <ignoreVersions>
        <ignoreVersion type="regex">.*[-.]RC.*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]rc.*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]Beta.*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]beta.*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]Alpha.*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]alpha.*</ignoreVersion>
        <ignoreVersion type="regex">.*[-.]M[0-9]+.*</ignoreVersion>
        <ignoreVersion type="regex">.*-SNAPSHOT</ignoreVersion>
        <ignoreVersion type="regex">.*\.CR[0-9]+.*</ignoreVersion>
    </ignoreVersions>

    <rules>
        <!-- Kotlin は RC 版を許可（現在使用中のため） -->
        <rule groupId="org.jetbrains.kotlin" comparisonMethod="maven">
            <ignoreVersions>
                <ignoreVersion type="regex">.*-SNAPSHOT</ignoreVersion>
            </ignoreVersions>
        </rule>

        <!-- Spring Boot は正式リリース版のみ -->
        <rule groupId="org.springframework.boot" comparisonMethod="maven">
            <ignoreVersions>
                <ignoreVersion type="regex">.*[-.]RC.*</ignoreVersion>
                <ignoreVersion type="regex">.*[-.]M[0-9]+.*</ignoreVersion>
                <ignoreVersion type="regex">.*-SNAPSHOT</ignoreVersion>
            </ignoreVersions>
        </rule>

        <!-- Spring Cloud は正式リリース版のみ -->
        <rule groupId="org.springframework.cloud" comparisonMethod="maven">
            <ignoreVersions>
                <ignoreVersion type="regex">.*[-.]RC.*</ignoreVersion>
                <ignoreVersion type="regex">.*[-.]M[0-9]+.*</ignoreVersion>
                <ignoreVersion type="regex">.*-SNAPSHOT</ignoreVersion>
            </ignoreVersions>
        </rule>
    </rules>

</ruleset>

